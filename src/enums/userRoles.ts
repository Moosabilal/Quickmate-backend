export enum Roles {
  USER = "Customer",
  PROVIDER = "ServiceProvider",
  ADMIN = "Admin",
}

export enum PaymentMethod {
  CARD = "Card",
  WALLET = "Wallet",
  UPI = "UPI",
  BANK = "Bank",
}

export enum PaymentStatus {
  PAID = "Paid",
  UNPAID = "UnPaid",
  REFUNDED = "Refunded",
}
